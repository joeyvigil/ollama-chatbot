# Copy to .env in the project root for docker compose:
#   cp .env.docker.example .env

OLLAMA_MODEL=llama3.2
HOST_PORT=8080
# Set to your public URL when deploying behind a domain (comma-separated)
CORS_ORIGINS=http://localhost:8080
